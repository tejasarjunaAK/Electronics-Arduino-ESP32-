double a=1000;
void setup(){
  pinMode(11,OUTPUT);
}
void loop(){
  for(int b=0;b<256;b++){
    analogWrite(11,b);
    delay(a/(255*2));}
  for(int b=255;b>=0;b--){
    analogWrite(11,b);
    delay(a/(255*2));}}
